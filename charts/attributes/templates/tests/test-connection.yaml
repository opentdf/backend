apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atttributes.fullname" . }}-test-connection"
  labels:
    {{- include "atttributes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atttributes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
