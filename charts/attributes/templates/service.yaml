apiVersion: v1
kind: Service
metadata:
  name: {{ include "atttributes.fullname" . }}
  labels:
    {{- include "atttributes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atttributes.selectorLabels" . | nindent 4 }}
