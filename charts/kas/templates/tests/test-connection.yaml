apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "access.name" . }}-test-connection"
  labels:
    {{- include "access.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "access.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
