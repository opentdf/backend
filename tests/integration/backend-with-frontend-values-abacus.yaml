basePath: ""
oidc:
  clientId: dcr-test
  queryRealms: tdf
ingress:
  enabled: true
  className: nginx
  hosts:
    - host: localhost
      paths: &paths
        - path: /
          pathType: Prefix
    - host: host.docker.internal
      paths: *paths
    - host: opentdf.local
      paths: *paths