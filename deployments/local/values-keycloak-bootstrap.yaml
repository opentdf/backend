# Development image for minikube - for nonlocal envs, override this with a proper version
# This should be preloaded into minikube by a local setup script
image:
  name: opentdf/tdf-identity-bootstrap:virtrulocal
  pullPolicy: Never
